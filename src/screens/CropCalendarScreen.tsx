import React from 'react';
import CropCalendar from '../components/CropCalendar';

const CropCalendarScreen = ({ navigation }) => {
  return <CropCalendar navigation={navigation} />;
};

export default CropCalendarScreen;