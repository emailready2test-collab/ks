import React from 'react';
import WeatherAlerts from '../components/WeatherAlerts';

const WeatherAlertsScreen = ({ navigation }) => {
  return <WeatherAlerts navigation={navigation} />;
};

export default WeatherAlertsScreen;